import React, { Component } from 'react'
import SuperMarketTemplate from './SuperMarketTemplate';

export default class SuperMarket extends Component {
  render() {
    return (
      <SuperMarketTemplate items={null} basket={null} />
    )
  }
}
